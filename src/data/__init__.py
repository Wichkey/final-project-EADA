"""Data loading and preprocessing utilities for the mule-detection project.

Submodules:

* :mod:`src.data.preprocess` – clean & aggregate the raw CSVs in
  ``data/raw`` and write the tidy outputs to ``data/processed``.
"""
